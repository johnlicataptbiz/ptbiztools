{
  "name": "crawl-site",
  "description": "Crawl a production site using Firecrawl CLI",
  "handler": "./crawl-site-skill.ts",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "URL of the production site to crawl"
      },
      "mode": {
        "type": "string",
        "description": "Crawling mode (single, sitemap, links)",
        "enum": ["single", "sitemap", "links"]
      },
      "maxPages": {
        "type": "number",
        "description": "Maximum number of pages to crawl",
        "default": 50
      },
      "includeLinks": {
        "type": "boolean",
        "description": "Whether to include linked pages in crawling",
        "default": false
      },
      "excludePatterns": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "URL patterns to exclude from crawling"
      }
    },
    "required": ["url"]
  }
}