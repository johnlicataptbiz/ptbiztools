{
  "name": "firecrawl-init",
  "description": "Initialize Firecrawl CLI with all features and browser support",
  "handler": "./skills/firecrawl-init.ts",
  "parameters": {
    "type": "object",
    "properties": {
      "directory": {
        "type": "string",
        "description": "Directory where to initialize Firecrawl CLI (defaults to current directory)"
      },
      "allFeatures": {
        "type": "boolean",
        "description": "Whether to initialize with all features (--all flag)",
        "default": true
      },
      "browser": {
        "type": "boolean",
        "description": "Whether to enable browser support (--browser flag)",
        "default": true
      }
    },
    "required": ["allFeatures", "browser"]
  }
}