# Optional. If unset, agent workflow falls back to deterministic local blueprint synthesis.
OPENAI_API_KEY=

# Existing PT Biz backend API base used by local-first sync pull route.
# Example: http://localhost:3000/api
PTBIZ_BACKEND_URL=http://localhost:3000/api
