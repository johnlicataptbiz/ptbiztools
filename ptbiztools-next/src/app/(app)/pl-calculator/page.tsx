import { PlaceholderToolPage } from "@/components/layout/placeholder-tool-page";

export default function PlCalculatorPage() {
  return (
    <PlaceholderToolPage
      title="P&L Calculator"
      description="This path is pinned for Danny's latest P&L calculator implementation during the migration. Legacy calculator behavior is intentionally not reintroduced."
    />
  );
}
