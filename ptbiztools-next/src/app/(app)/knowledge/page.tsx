import { PlaceholderToolPage } from "@/components/layout/placeholder-tool-page";

export default function KnowledgePage() {
  return (
    <PlaceholderToolPage
      title="Knowledge"
      description="Knowledge center route is mounted in Next and queued for full search, category, and document tooling migration."
    />
  );
}
